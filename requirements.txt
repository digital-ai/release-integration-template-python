# Python dependencies for this Digital.ai Release integration.
#
# The Release SDK is the only package you need to pin here. Everything the
# integration relies on at runtime is installed transitively with it:
#
#   - digitalai-release-api-client  (bundled with the SDK — never pin it separately)
#   - requests                      (a dependency of the SDK — no explicit entry needed)
#
# Add any packages your own scripts import below the SDK pin.

digitalai-release-sdk==26.3.0
